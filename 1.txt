Действуй как Senior Fullstack Developer, эксперт по Next.js и производительности веб-приложений. 

Я разрабатываю онлайн-кинотеатр под названием "spacefeel". 
Мой стек: Next.js (App Router), Tailwind CSS, API TMDB (для метаданных) и API YummyAnime (для контента).

### Проблема:
Сайт работает медленно: 
1. Долгая загрузка изображений (LCP страдает).
2. Медленные переходы между категориями (клиент долго "думает" перед отображением контента).
3. Зависимость от скорости ответов сторонних API (TMDB/YummyAnime).

### Что мне нужно:
1. Оптимизация Fetching: Покажи пример кода для серверного компонента с использованием продвинутого кэширования (fetch cache, tags, revalidate) для списков фильмов.
2. Изображения: Как правильно настроить компонент `next/image` для постеров TMDB, чтобы они грузились мгновенно (srcset, размеры, приоритеты)?
3. UX/Загрузка: Реализация Skeleton Screens через `loading.tsx` и использование `Suspense` для тяжелых частей страницы.
4. API Proxy: Пример создания внутреннего API-роута в Next.js для агрегации данных из TMDB и YummyAnime, чтобы уменьшить количество запросов с клиента.
5. Параллельные запросы: Как эффективно использовать `Promise.all` для одновременного получения данных из разных источников.

Пиши код на TypeScript. Давай только проверенные решения, которые реально ускорят проект "spacefeel".